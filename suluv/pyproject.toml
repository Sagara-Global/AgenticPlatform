[project]
name = "suluv"
version = "0.1.0"
description = "Suluv — Agentic Business Process Framework"
requires-python = ">=3.11"

[tool.uv.workspace]
members = ["packages/*"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
